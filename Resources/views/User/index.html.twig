{% extends '::base.html.twig' %}
{% block users %}class="active"{% endblock %}
{% block body -%}
    <div class="row">
        <div class="col-md-10 col-md-offset-1">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <a href="{{ path('cms_user_new') }}" class="btn btn-sm btn-success pull-right">Create a new
                        entry</a>

                    <h2 class="panel-title">User list</h2>
                </div>
                <div class="panel-body">
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th>Id</th>
                            <th>Username</th>
                            <th>Password</th>
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for entity in entities %}
                            <tr>
                                <td><a href="{{ path('cms_user_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                                <td>{{ entity.username }}</td>
                                <td>{{ entity.password|slice(0,20) }}</td>
                                <td>
                                    <a href="{{ path('cms_user_show', { 'id': entity.id }) }}"
                                       class="btn btn-sm btn-default btn-info">show</a>
                                    <a href="{{ path('cms_user_edit', { 'id': entity.id }) }}"
                                       class="btn btn-sm btn-default btn-warning">edit</a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
