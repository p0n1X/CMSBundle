{% extends '::base.html.twig' %}
{% block users %}class="active"{% endblock %}
{% block body -%}
    <div class="row">
        <div class="col-md-9 col-md-offset-1">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h2 class="panel-title">User</h2>
                </div>
                <div class="panel-body">
                    <table class="table table-striped">
                        <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ entity.id }}</td>
                        </tr>
                        <tr>
                            <th>Username</th>
                            <td>{{ entity.username }}</td>
                        </tr>
                        <tr>
                            <th>Password</th>
                            <td>{{ entity.password|slice(0,20) }}</td>
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <a href="{{ path('cms_user') }}" class="btn btn-sm btn-default pull-left">
                Back to the list
            </a>
            <span class=" pull-right">{{ form(delete_form) }}</span>
            <a href="{{ path('cms_user_edit', { 'id': entity.id }) }}" class="btn btn-sm btn-warning pull-right">
                Edit
            </a>
        </div>
    </div>
{% endblock %}
